<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Hand-authored to override @bacons/apple-targets' generated plist.
       RPBroadcastProcessMode is declared in BOTH locations ON PURPOSE — do not
       "de-dupe" it or App Store submission / device capture will break:
         1. Inside NSExtensionAttributes — iOS runtime only registers the
            broadcast extension (record button works) when the mode is here;
            misplaced, the record button no-ops silently.
         2. Directly under NSExtension — Apple's altool App Store validator
            looks for the key HERE. Absent, submission fails validation with
            "the key was not specified" (RPBroadcastProcessMode / 409). -->
  <key>NSExtension</key>
  <dict>
    <key>NSExtensionAttributes</key>
    <dict>
      <key>RPBroadcastProcessMode</key>
      <string>RPBroadcastProcessModeSampleBuffer</string>
    </dict>
    <key>NSExtensionPointIdentifier</key>
    <string>com.apple.broadcast-services-upload</string>
    <key>NSExtensionPrincipalClass</key>
    <string>$(PRODUCT_MODULE_NAME).SampleHandler</string>
    <key>RPBroadcastProcessMode</key>
    <string>RPBroadcastProcessModeSampleBuffer</string>
  </dict>
</dict>
</plist>
