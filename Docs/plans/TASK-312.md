# TASK-312: Arena vote reveal polish: advance-bar duration on late responses + guest empty-state signup CTA

**Status:** Draft
**Priority:** P4

---

## Objective
Two cosmetic gaps from the 2026-07-02 launch review: (1) when a slow vote response extends the reveal window (MIN_REVEAL_WITH_DATA_MS), the countdown drain bar has already emptied and sits at zero while the timer still runs — restart or extend the animation; (2) a guest hitting the 'No matchups yet' empty state is routed to My Teams which only says 'Sign in to see your teams' — a guest-aware sign-up CTA would close the funnel in one hop instead of two.

## Dependencies
None

## Open Questions
<!-- Unknowns or decisions to resolve before planning. Delete if none. -->
