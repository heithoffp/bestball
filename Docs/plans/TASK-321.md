# TASK-321: Parse engine v1: Underdog pick-ledger OCR with remote parse templates

**Status:** Draft
**Priority:** P2

---

## Objective
Swift parse engine per ADR-021: Vision OCR over template-defined regions of interest, fuzzy matching against the slate player pool, monotonic pick ledger, DraftState derivation (contract from chrome-extension/src/adapters/interface.js), per-pick confidence scoring, calibration-sweep recovery flow for mid-draft joins. Includes the remote template infrastructure (Supabase-served versioned JSON templates, client fetch/cache) and a screenshot fixture test corpus from real Underdog drafts.

## Dependencies
None

## Open Questions
<!-- Unknowns or decisions to resolve before planning. Delete if none. -->
