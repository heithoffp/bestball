simulation/Historical_Data/*.csv

# Local env files (service role keys, secrets)
.env.local
.env.*.local

# APNs auth keys and other local secrets (never commit .p8 private keys)
supabase/.secrets/
*.p8

# Script-local installs
scripts/node_modules

# Claude Code transient runtime artifacts
.claude/scheduled_tasks.lock

# Admin script output (user data dumps, etc.)
tmp/

# Raw spike/test screen recordings (kept local; stills committed as evidence)
mobile-app/docs/*.mp4

# Local-only draft recordings (>100MB, exceed GitHub limits; OCR corpus is checked in instead)
mobile-app/docs/live_draft_recording/
